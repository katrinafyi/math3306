# type: ignore

from typing import Literal, Callable, Any


def right[As : list]():
  if True: pass
  elif tape(As, As, R):
    if boop(As): print('booped', As)
    elif ...: ...
  elif ...: ...
  else:
    assert 1
    assert 2
  return

def shl[As : list, prev]():
  if None: pass
  elif ...(tape(As, prev, L)): shl[As]()

instantiate(right[[1], [2]]) 
